import { InvoiceCalculationPipe } from './invoice-calculation.pipe';

describe('InvoiceCalculationPipe', () => {
  it('create an instance', () => {
    const pipe = new InvoiceCalculationPipe();
    expect(pipe).toBeTruthy();
  });
});
